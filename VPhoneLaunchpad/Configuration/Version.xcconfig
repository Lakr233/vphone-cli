MARKETING_VERSION = 0.1.0
// Bump whenever the helper changes. The app blesses the helper again when the
// running helper's CFBundleVersion differs from the copy it embeds.
CURRENT_PROJECT_VERSION = 1
